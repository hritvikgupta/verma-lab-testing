- id: clean-config-files
  name: Clean Config Files
  entry: python3 clean_paths_files.py
  language: system
  args: ["/PMBB", "/home/user", "/var/data", "/project", "--directories=src,tests,configs"]
  stages: [commit]
